<UserControl x:Class="Agt.Desktop.Views.ComponentLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.DataContext>
        <StaticResource ResourceKey="FieldCatalog"/>
    </UserControl.DataContext>

    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="6" Padding="8" Background="#242424" CornerRadius="8"
                  MouseMove="Item_MouseMove">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" FontSize="16" Width="24" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding DisplayName}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
