<Window x:Class="Agt.Desktop.Views.NewProcessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nový proces" Height="180" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Název procesu:" Margin="0,0,0,6"/>
        <TextBox x:Name="NameBox" Grid.Row="1" MinWidth="300" Height="28" />

        <StackPanel Grid.Row="2" Margin="0,12,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="ErrorText"
                       VerticalAlignment="Center"
                       Foreground="Tomato"
                       Margin="0,0,16,0"
                       TextWrapping="Wrap"/>
            <Button Content="Zrušit" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Vytvořit" Width="90" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
