<UserControl x:Class="Agt.Desktop.Views.DesignerCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:Agt.Desktop.Models"
             xmlns:local="clr-namespace:Agt.Desktop.Views"
             Loaded="AnchorDock_OnLoaded"
             Unloaded="AnchorDock_OnUnloaded"
             SizeChanged="AnchorDock_OnSizeChanged">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Agt.Desktop;component/Views/Components/ComponentTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:FieldTemplateSelector x:Key="CompSelector" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <ItemsControl x:Name="ItemsHost"
                      ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="{DynamicResource EditorGridBrush}"
                            AllowDrop="True"
                            ContextMenuOpening="Canvas_ContextMenuOpening"
                            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                            MouseMove="Canvas_MouseMove"
                            MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                            MouseLeave="Canvas_MouseLeave"
                            LostMouseCapture="Canvas_LostMouseCapture"
                            DragOver="RootCanvas_DragOver"
                            Drop="RootCanvas_Drop"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top"  Value="{Binding Y}"/>
                    <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="{Binding Width}"
                          Height="{Binding Height}"
                          PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">

                        <!-- ⬇️ všechny komponenty přes univerzální šablony; mód = Run -->
                        <ContentControl Content="{Binding}"
                                        ContentTemplateSelector="{StaticResource CompSelector}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        local:RenderModeHelper.Mode="{x:Static m:RenderMode.Edit}"/>

                        <!-- selection overlay -->
                        <Border BorderBrush="{DynamicResource EditorAccentBrush}"
                                BorderThickness="2"
                                CornerRadius="2"
                                IsHitTestVisible="False"
                                Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>

                        <!-- move -->
                        <Thumb Opacity="0.01"
                               Background="Transparent"
                               DragStarted="MoveThumb_DragStarted"
                               DragDelta="MoveThumb_DragDelta"
                               DragCompleted="MoveThumb_DragCompleted"/>

                        <!-- resize -->
                        <Thumb Width="8" Height="8"
                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               Background="{DynamicResource EditorSurfaceAltBrush}"
                               Cursor="SizeNWSE"
                               DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Canvas x:Name="LassoLayer" IsHitTestVisible="False"/>
    </Grid>
</UserControl>
