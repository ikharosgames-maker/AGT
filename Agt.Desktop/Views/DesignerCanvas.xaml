<UserControl x:Class="Agt.Desktop.Views.DesignerCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:Agt.Desktop.Models"
             xmlns:local="clr-namespace:Agt.Desktop.Views"
             Loaded="AnchorDock_OnLoaded"
             Unloaded="AnchorDock_OnUnloaded"
             SizeChanged="AnchorDock_OnSizeChanged">


    <Grid>
        <!-- Items -->
        <ItemsControl x:Name="ItemsHost"
                  ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="{DynamicResource CanvasBackgroundBrush}"
                  AllowDrop="True"
                  ContextMenuOpening="Canvas_ContextMenuOpening"
                  MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                  MouseMove="Canvas_MouseMove"
                  MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                  MouseLeave="Canvas_MouseLeave"
                  LostMouseCapture="Canvas_LostMouseCapture"
                  DragOver="RootCanvas_DragOver"
                  Drop="RootCanvas_Drop"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top"  Value="{Binding Y}"/>
                    <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.Resources>

                <!-- LABEL -->
                <DataTemplate DataType="{x:Type m:LabelField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <!-- obsah -->
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,0">
                            <TextBlock Text="{Binding Label}"
                         Foreground="{Binding Foreground}"
                         FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"
                         VerticalAlignment="Center"/>
                        </Border>
                        <!-- selection overlay -->
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <!-- move/resize -->
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- TEXTBOX -->
                <DataTemplate DataType="{x:Type m:TextBoxField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Label}" Margin="0,0,0,4"
                           Foreground="{Binding Foreground}"
                           FontFamily="{Binding FontFamily}"/>
                                <TextBox Grid.Row="1"
                         Text="{Binding DefaultValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Foreground="{Binding Foreground}"
                         FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"
                         BorderThickness="0" VerticalContentAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- TEXTAREA -->
                <DataTemplate DataType="{x:Type m:TextAreaField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Label}" Margin="0,0,0,4"
                           Foreground="{Binding Foreground}"
                           FontFamily="{Binding FontFamily}"/>
                                <TextBox Grid.Row="1"
                         Text="{Binding DefaultValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Foreground="{Binding Foreground}"
                         FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"
                         BorderThickness="0" VerticalContentAlignment="Top"/>
                            </Grid>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- COMBOBOX -->
                <DataTemplate DataType="{x:Type m:ComboBoxField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Label}" Margin="0,0,0,4"
                           Foreground="{Binding Foreground}"
                           FontFamily="{Binding FontFamily}"/>
                                <ComboBox Grid.Row="1"
                          IsEditable="{Binding IsEditable}"
                          ItemsSource="{Binding Options}"
                          Foreground="{Binding Foreground}"
                          FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"/>
                            </Grid>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- CHECKBOX -->
                <DataTemplate DataType="{x:Type m:CheckBoxField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <CheckBox IsChecked="{Binding IsCheckedDefault}"
                        Content="{Binding Label}" VerticalAlignment="Center"
                        Foreground="{Binding Foreground}"
                        FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"/>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- DATE -->
                <DataTemplate DataType="{x:Type m:DateField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Label}" Margin="0,0,0,4"
                           Foreground="{Binding Foreground}"
                           FontFamily="{Binding FontFamily}"/>
                                <DatePicker Grid.Row="1"/>
                            </Grid>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

                <!-- NUMBER -->
                <DataTemplate DataType="{x:Type m:NumberField}">
                    <Grid Width="{Binding Width}" Height="{Binding Height}"
                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown">
                        <Border Background="{Binding Background}" BorderBrush="#666" BorderThickness="1" CornerRadius="2" Padding="6,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Label}" Margin="0,0,0,4"
                           Foreground="{Binding Foreground}"
                           FontFamily="{Binding FontFamily}"/>
                                <TextBox Grid.Row="1"
                         Text="{Binding DefaultValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Foreground="{Binding Foreground}"
                         FontFamily="{Binding FontFamily}" FontSize="{Binding FontSize}"
                         BorderThickness="0" VerticalContentAlignment="Center"/>
                            </Grid>
                        </Border>
                        <Border BorderBrush="#2E86FF" BorderThickness="2" CornerRadius="2"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                        <Thumb Opacity="0.01" Background="Transparent"
                   DragStarted="MoveThumb_DragStarted"
                   DragDelta="MoveThumb_DragDelta"
                   DragCompleted="MoveThumb_DragCompleted"/>
                        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Background="#999" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_DragDelta"/>
                    </Grid>
                </DataTemplate>

            </ItemsControl.Resources>
        </ItemsControl>

        <!-- Lasso overlay -->
        <Canvas x:Name="LassoLayer" IsHitTestVisible="False"/>
    </Grid>
</UserControl>
