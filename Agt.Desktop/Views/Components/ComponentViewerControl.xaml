<UserControl x:Class="Agt.Desktop.Views.ComponentViewerControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Agt.Desktop.Views">
    <!-- ŽÁDNÉ přebytečné řádky/okraje navíc -->
    <Grid Background="Transparent" SnapsToDevicePixels="True">
        <ItemsControl
            ItemsSource="{Binding ElementName=Root, Path=Items}"
            views:RenderModeHelper.Mode="{Binding ElementName=Root, Path=Mode}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- AutoSize + padding, aby nebyly prvky nalepené nahoře/ vlevo -->
                    <views:AutoSizeCanvas Padding="12"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left"  Value="{Binding X}"/>
                    <Setter Property="Canvas.Top"   Value="{Binding Y}"/>
                    <Setter Property="Panel.ZIndex" Value="{Binding ZIndex, FallbackValue=0}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
</UserControl>
