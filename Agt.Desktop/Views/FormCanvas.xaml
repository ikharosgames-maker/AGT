<UserControl x:Class="Agt.Desktop.Controls.FormCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:Agt.Desktop.Models"
             xmlns:views="clr-namespace:Agt.Desktop.Views"
             x:Name="Root"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">

    <Grid>
        <ItemsControl Width="{Binding CanvasWidth, ElementName=Root}"
                      Height="{Binding CanvasHeight, ElementName=Root}"
                      ItemsSource="{Binding Items, ElementName=Root}"
                      views:RenderModeHelper.Mode="{Binding Mode, ElementName=Root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left"  Value="{Binding X}"/>
                    <Setter Property="Canvas.Top"   Value="{Binding Y}"/>
                    <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>
    </Grid>
</UserControl>
