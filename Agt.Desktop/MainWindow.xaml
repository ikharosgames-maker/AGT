<Window x:Class="Agt.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Agt.Desktop.Views"
        Title="AGT – Návrhář formulářů" Width="1200" Height="800"
        Background="#121212" Foreground="#EEE"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Grid.ColumnSpan="3" Background="#1E1E1E">
            <TextBlock Text="AGT – Návrhář datových formulářů" Margin="12,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </DockPanel>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="#333" BorderThickness="0,0,1,0" Background="#1A1A1A">
            <views:ComponentLibraryView/>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1" Background="#181818">
            <views:DesignerCanvas/>
        </Grid>

        <Border Grid.Row="1" Grid.Column="2" BorderBrush="#333" BorderThickness="1,0,0,0" Background="#1A1A1A">
            <views:PropertiesPanel/>
        </Border>

        <StatusBar Grid.Row="2" Grid.ColumnSpan="3" Background="#1E1E1E">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText, FallbackValue=Připraveno}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
